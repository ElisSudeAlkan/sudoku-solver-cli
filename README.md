# sudoku-solver-cli

Local-only Sudoku solver CLI. No network access and no external APIs  required.

## Run
```bash
python main.py --input puzzles/mixed_formats.txt --strategy mrv --print-solved true